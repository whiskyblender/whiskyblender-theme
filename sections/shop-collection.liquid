<section class="banding">
  <article class="max">
    <h3 class="section-title-wb">{{ section.settings.collection.title }}</h3>
    <p>
      {{ section.settings.collection.description }}
    </p>
    <ul class="product-list">
      {%- for product in section.settings.collection.products -%}
        {% render 'product-display', product: product %}
      {% endfor %}
    </ul>
    <button onclick="{{ section.settings.collection.url }}" class="secondary-button">View all</button>
  </article>
</section>

{% schema %}
{
  "name": "Shop Collection",
    "presets": [{ "name": "Shop Collection" }],

  "settings": [
      {
        "type": "collection",
        "id": "collection",
        "label": "collection"
      }
  ],
}
{% endschema %}
